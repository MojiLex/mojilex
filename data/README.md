# Canonical records

This directory contains canonical collection, emoji, and membership records.
It is intentionally empty until a validated contribution is accepted. Empty
shard directories and empty bucket files are not committed.

See the repository root README and `schemas/v1/` for the path contract.
