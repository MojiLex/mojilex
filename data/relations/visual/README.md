# Reviewed visual relations

Only approved human decisions are canonical here. Files use
`data/relations/visual/{first two SHA-256(id) hex}/{next two hex}.jsonl`, and
records are sorted by relation ID. Candidate lists, previews, decoded media,
comparison sheets, and local similarity indexes must remain outside Git.
